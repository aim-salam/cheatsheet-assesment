import React from "react";
import { Button } from "@mui/material";
function AddTopicButton({ handleAddTopic }) {
  return <Button sx={{ mt: "auto" }}>Add topic</Button>;
}

export default AddTopicButton;
